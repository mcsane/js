const numerot = []
numerot.push(parseInt(prompt('anna numero')));
numerot.push(parseInt(prompt('anna numero')));
numerot.push(parseInt(prompt('anna numero')));
numerot.push(parseInt(prompt('anna numero')));
numerot.push(parseInt(prompt('anna numero')));
console.log(numerot)

for (let i = numerot.length -1; i >= 0; i-- ){
  console.log(numerot[i])
}
